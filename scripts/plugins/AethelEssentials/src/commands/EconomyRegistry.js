// Economy commands temporarily empty — PlaceBountyCommand and BountiesCommand
// moved to the BountyHunter plugin (aethel:bounty_hunter) to avoid overlap.
export const EconomyRegistry = {
    getCommands() {
        return [];
    }
};
